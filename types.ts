//export 외부에서 import로 접근 가능 
export interface Book {
    id: string;
    title: string;
    author: string;
}

//todo 인터페이스 
export interface TodoList{
    id: string;
    title: string;
    date: string;
}